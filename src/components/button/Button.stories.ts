import { Button } from "./Button";

export default {
  title: 'Collections/Button',
  component: Button,
  tags: ['autodocs'],
  argTypes: {},
};

export const Primary = {
  args: {
    variant: 'primary',
    label: 'Rechercher',
  },
};